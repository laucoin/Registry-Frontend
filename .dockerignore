# Unlike the CSR Dockerfile, this one builds inside the image (multi-stage), so it
# needs the full source — only exclude what the build itself doesn't need.
.git
.github
.idea
.vscode
*.iml
node_modules
dist
.angular
.env
.env.example
*.md
